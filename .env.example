# PostgreSQL
DATABASE_URL="postgresql://postgres:password@localhost:5432/ad_cnt?schema=public"

# Authentication secrets will be added in the auth implementation phase.
# AD connector settings will be kept in the internal connector, not exposed to the browser.
