# PostgreSQL
DATABASE_URL="postgresql://postgres:password@localhost:5432/ad_cnt?schema=public"

# Authentication: generate a random secret of at least 32 characters.
AUTH_SESSION_SECRET="replace-with-a-long-random-secret"

# Active Directory connector settings will be added later.
# Keep LDAP/AD credentials only on the internal connector/server environment.
