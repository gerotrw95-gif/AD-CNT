import "server-only";
import ldap from "ldapjs";
import { prisma } from "./prisma";
import { syncDirectoryGroups } from "./ad-group-sync";

export type DirectoryUser = { username: string; displayName: string; email?: string | null; department?: string | null; distinguishedName: string; adObjectGuid: string };
export type DirectoryGroup = { name: string; description?: string | null; members: string[] };
export type ADSyncResult = { users: number; groups: number; addedUsers: string[]; reactivatedUsers: string[]; deactivatedUsers: string[]; syncedGroups: string[]; errors: string[]; startedAt: string; finishedAt: string; durationMs: number };

function required(name: string) { const value = process.env[name]; if (!value) throw new Error(`Missing ${name}`); return value; }
function guidFromBuffer(value: unknown) { if (!Buffer.isBuffer(value) || value.length !== 16) return String(value ?? ""); const b=value; const h=(n:number)=>n.toString(16).padStart(2,"0"); return `${[b[3],b[2],b[1],b[0]].map(h).join("")}-${[b[5],b[4]].map(h).join("")}-${[b[7],b[6]].map(h).join("")}-${[b[8],b[9]].map(h).join("")}-${Array.from(b.subarray(10)).map(h).join("")}`; }
function values(entry:any,key:string): unknown[] { const a=entry.pojo?.attributes?.find((x:any)=>x.type?.toLowerCase()===key.toLowerCase())?.values; if(Array.isArray(a)) return a; const d=entry[key]; return Array.isArray(d)?d:d==null?[]:[d]; }
function first(entry:any,key:string){ return values(entry,key)[0]; }
async function search(client:ldap.Client,base:string,options:ldap.SearchOptions):Promise<any[]> { return new Promise((resolve,reject)=>{ client.search(base,options,(error,result)=>{ if(error) return reject(error); const rows:any[]=[]; result.on("searchEntry",(e:any)=>rows.push(e)); result.on("error",reject); result.on("end",(s:any)=>s?.status===0?resolve(rows):reject(new Error(`LDAP search failed: ${s?.status}`))); }); }); }
async function connect(){ const client=ldap.createClient({url:required("AD_LDAP_URL"),timeout:10000,connectTimeout:10000,tlsOptions:{rejectUnauthorized:process.env.AD_LDAP_REJECT_UNAUTHORIZED!=="false"}}); await new Promise<void>((resolve,reject)=>client.bind(required("AD_BIND_DN"),required("AD_BIND_PASSWORD"),(e)=>e?reject(e):resolve())); return client; }
async function readDirectory(client:ldap.Client){ const base=required("AD_BASE_DN"); const ue=await search(client,base,{scope:"sub",filter:"(&(objectCategory=person)(objectClass=user)(sAMAccountName=*))",attributes:["sAMAccountName","displayName","mail","department","distinguishedName","objectGUID"]}); const users:DirectoryUser[]=ue.map((e)=>({username:String(first(e,"sAMAccountName")??"").toLowerCase(),displayName:String(first(e,"displayName")??first(e,"sAMAccountName")??""),email:first(e,"mail")?String(first(e,"mail")):null,department:first(e,"department")?String(first(e,"department")):null,distinguishedName:String(first(e,"distinguishedName")??e.dn??""),adObjectGuid:guidFromBuffer(first(e,"objectGUID"))})).filter(u=>u.username&&u.distinguishedName&&u.adObjectGuid); const ge=await search(client,base,{scope:"sub",filter:"(&(objectCategory=group)(cn=*))",attributes:["cn","description","member"]}); const groups:DirectoryGroup[]=ge.map((e)=>({name:String(first(e,"cn")??""),description:first(e,"description")?String(first(e,"description")):null,members:values(e,"member").map(String)})).filter(g=>g.name); return {users,groups}; }

export async function syncActiveDirectory(): Promise<ADSyncResult> { const started=new Date(); const errors:string[]=[]; const addedUsers:string[]=[]; const reactivatedUsers:string[]=[]; const deactivatedUsers:string[]=[]; const syncedGroups:string[]=[]; const client=await connect(); try { const {users,groups}=await readDirectory(client); if(!users.length) throw new Error("AD returned zero users; sync aborted to protect existing accounts"); const deptCache=new Map<string,string>(); const existing=await prisma.user.findMany({where:{source:"ACTIVE_DIRECTORY"},select:{adObjectGuid:true,status:true,username:true}}); const existingByGuid=new Map(existing.filter(u=>u.adObjectGuid).map(u=>[u.adObjectGuid!,u])); for(const u of users){ try { let departmentId:string|undefined; const name=u.department?.trim(); if(name){ departmentId=deptCache.get(name); if(!departmentId){ const d=await prisma.department.upsert({where:{name},update:{},create:{name}}); departmentId=d.id; deptCache.set(name,d.id); } } const old=existingByGuid.get(u.adObjectGuid); await prisma.user.upsert({where:{adObjectGuid:u.adObjectGuid},update:{username:u.username,displayName:u.displayName,email:u.email,source:"ACTIVE_DIRECTORY",distinguishedName:u.distinguishedName,departmentId,status:"ACTIVE",lastAdSyncAt:started},create:{username:u.username,displayName:u.displayName,email:u.email,source:"ACTIVE_DIRECTORY",adObjectGuid:u.adObjectGuid,distinguishedName:u.distinguishedName,departmentId,status:"ACTIVE",lastAdSyncAt:started}}); if(!old) addedUsers.push(u.username); else if(old.status!=="ACTIVE") reactivatedUsers.push(u.username); } catch(e){ errors.push(`User ${u.username}: ${e instanceof Error?e.message:String(e)}`); } } const activeGuids=users.map(u=>u.adObjectGuid); const deactivated=await prisma.user.findMany({where:{source:"ACTIVE_DIRECTORY",adObjectGuid:{notIn:activeGuids},status:"ACTIVE"},select:{username:true}}); if(deactivated.length) deactivatedUsers.push(...deactivated.map(u=>u.username)); await prisma.user.updateMany({where:{source:"ACTIVE_DIRECTORY",adObjectGuid:{notIn:activeGuids},status:"ACTIVE"},data:{status:"INACTIVE",lastAdSyncAt:started}}); const byDn=new Map(users.map(u=>[u.distinguishedName.toLowerCase(),u.username])); const normalized=groups.map(g=>({...g,members:g.members.map(d=>byDn.get(d.toLowerCase())).filter((x):x is string=>Boolean(x))})); try { const groupResult=await syncDirectoryGroups(normalized); syncedGroups.push(...normalized.slice(0,groupResult.synced).map(g=>g.name)); } catch(e){ errors.push(`Groups: ${e instanceof Error?e.message:String(e)}`); } const finished=new Date(); return {users:users.length,groups:groups.length,addedUsers,reactivatedUsers,deactivatedUsers,syncedGroups,errors,startedAt:started.toISOString(),finishedAt:finished.toISOString(),durationMs:finished.getTime()-started.getTime()}; } finally { client.unbind(); } }
